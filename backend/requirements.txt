# FastAPI and Server
fastapi
uvicorn[standard]

# MongoDB
motor
pymongo[srv]

# Pydantic for data validation
pydantic

# Environment variables
python-dotenv

# CORS support (included with starlette via fastapi)
python-multipart
